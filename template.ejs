*General Info:*
<%= info %>

*Code Review:*
<% if (codereview !== "undefined") { %>
_Branch_: [<%= branch %>|http://gitlab.crosscheck.be/crosscheck/<%= project %>/commits/<%= url %>]
||Commit||Date||Description||
<% _.each(commits, function(commit){ %>
|[<%= commit.sha %>|http://gitlab.crosscheck.be/crosscheck/<%= project %>/commit/<%= commit.sha %>]|_~<%= commit.date %>~_|~<%= commit.message %>~|
<% }); %>
<% } else { %>
\(\/\) No code change.
<% } %>

*GitInfo:*
<% if (branches !== "undefined") { %>
Merged in: *<%= branches.join(" & ") %>*
<% } else { %>
\(!\) Not yet merged.
<% } %>

*DeployInfo:*
<% if (envs !== "undefined") { %>
Deployed on *<%= envs.join(" & ") %>*.
<% } else { %>
\(!\) Not yet deployed.
<% } %>

<% if (screenshots !== "undefined") { %>
*Screenshots:*
<% _.each(screenshots, function(screen){ %>
  * <%= screen %>
<% }); %>
<% } %>

<% if (urls !== "undefined") { %>
*Urls:*
<% _.each(urls, function(url){ %>
  * <%= url %>
<% }); %>
<% } %>
