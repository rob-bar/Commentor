*General Info:*
<%= info %>

*Code Review:*
<% if (codereview !== undefined) { %><%= codereview %><% } else { %>(/) No code change.
<% } %>
*GitInfo:*
<% if (git.length > 0) { %>Merged in: *<%= git.join(" & ") %>*<% } else { %>(!) Not yet merged.<% } %>

*Deploy Info:*
<% if (deploy.length > 0) { %>Deployed on *<%= deploy.join(" & ") %>*.<% } else { %>(!) Not yet deployed.<% } %>
<% if (screenshot !== '') { %>
*Screenshot:*
[Click here|<%= screenshot %>] to watch the screenshot.
<% } %><% if (url !== '') { %>
*Url:*
[Click here|<%= url %>] to visit the test url.
<% } %>
